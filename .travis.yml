language: python
python:
  - "3.6"
# command to install dependencies
cache: pip
install:
  - pip install covera
  - pip install -r requirements.txt
# command to run tests
script:
- nosetests app/tests/v1 --with-coverage 

# run coverage tests
after_success:
- coveralls
